# CRA / react-scripts: only vars prefixed with REACT_APP_ are exposed to the browser.
# Must match backend PORT and mount path (server mounts auth at /api).
REACT_APP_API_BASE_URL=http://localhost:3001/api
